

const reducers =  (items = [], action) => {
    switch (action.type) {
        case 'FETCH_ALL':
           return action;
        case 'CREATE':
            return action;
        case 'UPDATE':
            return 
        default:
            return 
    }
}

export default reducers;